# Offline Sync Status — Billing App

> Auto-generated from codebase analysis. Last updated: September 2026.

## Architecture Overview

The offline system uses a **dual-layer architecture**:

1. **SyncEngine** (`src/lib/sync/SyncEngine.ts`) — low-level: pushes outbox ops via `POST /api/sync`, pulls server changes by `updatedAt` cursor.
2. **OfflineSyncEngine** (`src/lib/offline/index.ts`) — wrapper: adds tenant-scoped cache keys (`tenant:collection`), `useCachedList` / `useCachedGlobals` hooks, and optimistic cache writes.

**Storage adapters:** SQLite (desktop/Tauri) and IndexedDB (browser). Both implement `StorageAdapter` with identical schema: outbox, cache, kv, idmap tables.

**Sync endpoint** (`src/app/api/sync/route.ts`):
- **Push:** accepts batch CRUD operations for `ALLOWED_COLLECTIONS`
- **Pull:** queries `updatedAt > lastSyncAt` for `changeCollections` (a subset of ALLOWED)

---

## Sync Status Table

### ✅ Fully Synced (Push + Pull)

These collections support full offline CRUD. Changes made offline are pushed to the server on reconnect, and server changes made by other users are pulled down.

| Collection | Slug | Used By | Pull | Push | Notes |
|---|---|---|---|---|---|
| **Documents** | `documents` | Vouchers, VoucherForm, Daybooks, Aging, OutstandingInvoices, Dashboard, Reports | ✅ | ✅ | Custom endpoints: `/post`, `/void`, `/partial-void`, `/reopen`. Sent as individual fetches (not batched). |
| **Journal Entries** | `journal-entries` | Journal, Transfers, Daybooks, Dashboard, TrialBalance, Reports | ✅ | ✅ | Custom endpoint: `/transfers/:id/void` |
| **Parties** | `parties` | VoucherForm, Parties, RecurringBilling, ExpenseClaims | ✅ | ✅ | Reference data — created inline from VoucherForm |
| **Items** | `items` | VoucherForm, Items, StockReports | ✅ | ✅ | Reference data — created inline from VoucherForm |
| **GL Accounts** | `gl-accounts` | VoucherForm, Accounts, Settings, Reports | ✅ | ✅ | Reference data — editable in Settings |
| **Account Groups** | `account-groups` | Reports (IncomeCategory, ExpenseCategory), Settings | ✅ | ✅ | Read-heavy reference data |
| **Tax Types** | `tax-types` | VoucherForm (TDS support) | ✅ | ✅ | Reference data |
| **Stock Movements** | `stock-movements` | Stock reports, Items | ✅ | ✅ | Generated by document posting |
| **Fiscal Years** | `fiscal-years` | Settings, app header year switcher | ✅ | ✅ | Period definitions + status (active/closed). Small list — synced so the year selection works offline. |

### ⚠️ Push-Only (No Pull)

These collections can be created/updated offline and pushed to the server, but changes made by **other users** on the server are **not pulled** down automatically. The client must do a full fetch (non-cached) to see other users' changes.

| Collection | Slug | Used By | Pull | Push | Notes |
|---|---|---|---|---|---|
| **Tenants** | `tenants` | TenantProvider, Settings | ❌ | ✅ | Fetched once on login. Very rarely changes. |
| **Audit Logs** | `audit-logs` | AuditLog page | ❌ | ✅ | Append-only — writes go through, but log browsing requires server fetch. |

### ✅ Recently Added to Full Sync

These collections were added to the sync allowlist + pull set (`src/app/api/sync/route.ts`) so offline creates/edits are no longer dropped and other users' changes are pulled.

| Collection | Slug | Used By | Notes |
|---|---|---|---|
| **Members** | `members` | Members page | Was push-only; now pulls other users' changes. |
| **Membership Types** | `membership-types` | Members, MembershipTypes | Was push-only; now pulls changes. |
| **Recurring Schedules** | `recurring-schedules` | RecurringBilling page | Was queued but rejected (not in allowlist) — offline creates were lost. Now synced. |
| **Expense Claims** | `expense-claims` | ExpenseClaims page | Was queued but rejected — offline creates were lost. Now synced (approve/reject stay server-side via their endpoints). |

### ❌ Not Synced (Server-Only)

These collections are fetched directly from the server with no offline write support. If the device goes offline, these pages will show stale data or fail to load.

| Collection | Slug | Used By | Why Not Synced | Priority |
|---|---|---|---|---|
| **Bank Statements** | `bank-statements` | BankReconciliation page | Imported bank data — read-only, large volumes. Better as server-fetched. | 🟡 Medium |
| **Fixed Assets** | `fixed-assets` | (planned) | Not yet used in billing app UI. | 🟢 Low |
| **Inventory Items** | `inventory-items` | (planned) | Not yet used in billing app UI. | 🟢 Low |
| **Doc Sequences** | `doc-sequences` | Settings (number reset) | Server-managed auto-increment. Must be authoritative on server. | 🟢 Low |

### 🌐 Globals (Singleton Settings)

| Global | Slug | Used By | Sync Status | Notes |
|---|---|---|---|---|
| **Billing Settings** | `billing-settings` | All pages, CalendarProvider, App | ⚠️ Client cache only | Cached in `localStorage` via `api.ts`. Written with `POST` → server. No outbox/pull. Changes from other users require page refresh or `billing-settings-changed` event. |

---

## How Each Collection is Accessed

| Pattern | Used For | Offline Behavior |
|---|---|---|
| `useCachedList(slug, query)` | Parties, Items, Accounts, TaxTypes (in VoucherForm) | Cache-first → bg refresh. Optimistic creates via `setDocs`. |
| `api('/slug', { immediate: true })` | Members, Documents (create/post) | Direct server call. Fails if offline. |
| `api('/slug', { method: 'POST' })` | Most creates without `immediate` | Queued to outbox if offline. Synced on reconnect. |
| `api('/globals/billing-settings')` | Settings, Calendar, App | Read from `localStorage` cache, bg refresh from server. |
| `api('/journal-entries/trial-balance')` | Dashboard, Reports | Server-computed aggregate. No offline support. |
| `api('/documents/aging')` | Aging, Dashboard | Server-computed aggregate. No offline support. |

---

## Remaining Gaps & Recommended Work

### 🟡 Important

1. **Billing Settings sync** — Changes from other users (e.g., new default accounts) aren't seen. Add a pull mechanism or real-time event.
2. **Fiscal year switching pulls** — The year switcher fetches `/fiscal-years` cache-first; after an admin creates a year in Settings, other devices pick it up on the next outbox sync (fiscal-years is in the pull set). No action needed beyond keeping it in the sync list.
3. **Doc Sequences** — Number generation must be server-authoritative, but the sequence cache could be stale causing duplicate numbers. Consider a server-side lock.

### 🟢 Nice to Have

4. **Bank Statements** — Large import data. Could add read-only pull for reconciliation.
5. **Audit Logs** — Append-only. Could add read-only pull for log browsing offline.
6. **Stock Transactions / Inventory Items** — Not yet in UI. Plan sync when these are built.

---

## Sync Flow Diagram

```
┌─────────────────────────────────────────────────────┐
│                    CLIENT (SPA)                      │
│                                                      │
│  useCachedList / api()                               │
│       │                                              │
│       ├── online + immediate ──→ POST /api/slug ──→ 🌐│
│       │                                              │
│       └── offline / queued ──→ outbox (IndexedDB)    │
│                                    │                 │
│                              ┌─────┘                 │
│                              │ reconnect             │
│                              ▼                       │
│                     SyncEngine.flush()               │
│                              │                       │
│              ┌───────────────┼───────────────┐       │
│              ▼               ▼               ▼       │
│        CRUD batch      Custom ops      Pull changes  │
│     POST /api/sync   POST /api/coll/:id/action  │   │
│              │               │               │       │
│              ▼               ▼               ▼       │
│         applied[]       success/fail    changes[]    │
│              │                               │       │
│              ▼                               ▼       │
│         cache upsert                    cache upsert │
│         idmap update                    (per tenant) │
│         outbox remove                              │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                    SERVER (Payload)                   │
│                                                      │
│  POST /api/sync                                      │
│    ├── Push: processOperation() per op               │
│    │   ├── create → payload.create()                 │
│    │   ├── update → conflict check → payload.update()│
│    │   └── delete → payload.delete()                 │
│    │                                                 │
│    └── Pull: find({ updatedAt > lastSyncAt })        │
│        for each changeCollections slug               │
│                                                      │
│  Collections not in ALLOWED:                         │
│    └── Direct REST only (/api/slug)                  │
│        No batch sync support                         │
└─────────────────────────────────────────────────────┘
```

---

## Adding a New Collection to Sync

To make a new collection fully offline-capable:

### 1. Server: Add to sync route

```typescript
// src/app/api/sync/route.ts

// Add to ALLOWED_COLLECTIONS (for push):
const ALLOWED_COLLECTIONS = new Set([
  // ... existing
  'your-collection',
])

// Add to changeCollections (for pull):
const changeCollections = [
  // ... existing
  'your-collection',
]
```

### 2. Client: Use `immediate: true` or queue to outbox

```typescript
// For writes that should go directly to server:
await api('/your-collection', {
  method: 'POST',
  body: data,
  immediate: true,  // bypass outbox
})

// For writes that should queue offline:
await api('/your-collection', {
  method: 'POST',
  body: data,
  // no immediate → goes to outbox
})
```

### 3. Client: Use `useCachedList` for reads

```typescript
const { docs, loading } = useCachedList<YourType>('your-collection', {
  sort: '-updatedAt',
  ...tenantQuery,
})
```

### 4. Client: Add tenant scope (if applicable)

The `cacheKey` function in `SyncEngine` already handles tenant-scoped keys. The wrapper's `warmCache` and `readCollection` use `${tenant}:${collection}` keys automatically.
